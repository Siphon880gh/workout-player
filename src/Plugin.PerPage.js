
export const runPerPage = () => {
    // Pointless to make exercise links open in same frame of wrapper-workout
    // because youtube and google blocked iframe embedding
    
    // document.querySelectorAll("a[target='_blank']").forEach(el=>{
    //     el.setAttribute("target", "_self");
    // });
} // runPerPage